#include <iostream>
#include <unordered_map>
#include <vector>

using namespace std;

int main(){
    int array[]= {1, 2, 3, 6, 1, 2, 3, 5, 4, 5, 6, 7, 8, 8, 4, 6, 4, 3, 2, 5, 7, 8, 9, 4, 7, 9, 2};
    vector<int> v;

    unordered_map<int, bool> output;
    int len = sizeof(array)/sizeof(array[0]);

    for(int i = 0 ; i < len; i++)
    {
       if(output[array[i]] == false)
       {
           vector.insert
       }
    }


}